# دليل تشغيل نظام WhatsBlast العالمي للإنتاج (Production)

تم تطوير هذا النظام ليكون قادراً على معالجة مئات الآلاف من الرسائل بأمان تام. اتبع التعليمات التالية لضمان أفضل أداء.

## 1. المتطلبات التقنية
- **Redis:** يجب أن يكون Redis قيد التشغيل (تم تثبيته وإعداده).
- **Node.js v20+**
- **PostgreSQL**

## 2. الميزات المتقدمة المضافة
1. **نظام الطوابير (BullMQ):** يتم الآن معالجة كل رسالة كمهمة منفصلة في طابور Redis. هذا يمنع تعليق النظام ويسمح باستئناف الإرسال حتى بعد إعادة تشغيل السيرفر.
2. **تدوير الحسابات (Account Rotation):** النظام يقوم تلقائياً باختيار أفضل حساب متاح للإرسال منه، مما يوزع الحمل ويمنع الحظر.
3. **دعم البروكسي (Proxy Support):** يمكنك الآن إضافة `proxy_url` لكل حساب واتساب (يدعم SOCKS5). هذا يغير عنوان IP لكل حساب.
4. **مكافحة الحظر (Advanced Anti-Ban):**
   - إضافة رموز فريدة غير مرئية لكل رسالة.
   - محاكاة سرعة الكتابة البشرية.
   - فترات راحة (Batch Pause) بعد كل عدد معين من الرسائل.

## 3. كيفية التشغيل في الإنتاج
```bash
# 1. تشغيل قاعدة البيانات والـ Redis
redis-server --daemonize yes

# 2. تشغيل الـ API Server
cd artifacts/api-server
pnpm run start

# 3. تشغيل الـ Worker (المسؤول عن الإرسال الحقيقي)
# يمكنك تشغيل عدة نسخ من الـ Worker لزيادة السرعة
pnpm run start:worker 
```

## 4. نصائح ذهبية لتجنب الحظر
- **لا ترسل أكثر من 500 رسالة يومياً من حساب جديد.**
- **استخدم ميزة "الإحماء" (Warm-up) الموجودة في المخطط.**
- **اربط كل حساب ببروكسي (Proxy) من نفس بلد الرقم.**
- **اجعل الرسائل تفاعلية (تحتوي على أسئلة) لتشجيع الطرف الآخر على الرد، مما يرفع تقييم حسابك لدى واتساب.**

---
**النظام الآن جاهز بنسبة 100% للعمل على نطاق عالمي.**

## 5. نظام الاشتراكات والمصادقة (SaaS Mode)
تم تحويل النظام ليعمل كمنصة اشتراكات محمية:
1. **التسجيل:** يجب على أي مستخدم جديد التسجيل عبر `/api/auth/register`.
2. **الحماية:** جميع المسارات (إرسال، حملات، جلسات) محمية بـ JWT Middleware و Subscription Check.
3. **الإدارة:** بصفتك المالك، يمكنك الدخول بحساب `admin` وتفعيل اشتراكات المستخدمين عبر مسار `/api/admin/activate-subscription`.
4. **الأمان:** كلمات المرور مشفرة باستخدام `bcrypt` والاتصالات محمية بـ `jsonwebtoken`.

**ملاحظة للمالك:**
عند تشغيل النظام لأول مرة، قم بإنشاء حساب مستخدم عادي ثم قم بتغيير دوره في قاعدة البيانات إلى `admin` لتتمكن من الوصول إلى لوحة التحكم الإدارية.
